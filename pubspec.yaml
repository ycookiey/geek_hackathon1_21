name: geek_hackathon1_21
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ^3.7.2

dependencies:
    flutter:
        sdk: flutter
    cupertino_icons: ^1.0.8
    google_maps_flutter: ^2.10.1
    geolocator: ^13.0.3
    supabase_flutter: ^2.0.0
    envied: ^0.5.3
    flutter_map: ^6.0.1
    latlong2: ^0.9.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0
    build_runner: ^2.4.8
    envied_generator: ^0.5.3

flutter:
    uses-material-design: true
    assets:
        - .env
